{{ header }}
{# Используем структуру хлебных крошек из cart.html с классами .crumbs и .crumb #}
<div class="crumbs">
  {% for breadcrumb in breadcrumbs %}
    <a href="{{ breadcrumb.href }}" class="crumb">{{ breadcrumb.text }}</a>
  {% endfor %}
</div>

{# Оборачиваем начало контента в нужные классы #}
<section class="page_default">
  <div class="container">
    <h1 class="page_title">{{ heading_title }}</h1>